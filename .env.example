# Postgres connection (used by app/api/db.js)
DATABASE_URL=postgres://USER:PASSWORD@HOST:5432/DBNAME

# Postgres connection parts (used by app/lib/create.js)
HOST=localhost
PGPORT=5432
PGDATABASE=postgres
PGUSER=postgres
PG_PASSWORD=your_password
PASSWORD=your_password

# Admin login (expected for /login route)
ADMIN_USER=admin
ADMIN_PASSWORD=change_me
